
services:
  # 交互式开发容器
  fuzzer:
    build: .
    image: at-fuzz:latest
    container_name: at-fuzz-dev
    volumes:
      - .:/fuzzer
    working_dir: /fuzzer
    stdin_open: true
    tty: true
    privileged: true  # 允许 bwrap 运行
    command: /bin/bash